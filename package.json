{
  "name": "abp-filter-linter",
  "displayName": "ABP Filter Linter",
  "description": "Validates Adblock Plus filter syntax in .txt files",
  "version": "0.4.1",
  "publisher": "manuel-martinez-dev",
  "repository": {
    "type": "git",
    "url": "https://github.com/manuel-martinez-dev/ABP-filter-linter"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "icon": "icon.png",
  "license": "MIT",
  "categories": ["Linters"],
  "activationEvents": [
    "onLanguage:plaintext"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "languages": [
      {
        "id": "plaintext",
        "extensions": [".txt"]
      }
    ]
  },
  "scripts": {
    "build": "esbuild src/extension.ts --bundle --outfile=out/extension.js --external:vscode --format=cjs --platform=node",
    "watch": "npm run build -- --watch",
    "test": "vitest run",
    "vscode:prepublish": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "esbuild": "^0.27.3",
    "typescript": "^5.3.0",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "css-what": "^6.1.0"
  }
}
